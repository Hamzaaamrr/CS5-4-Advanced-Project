package com.playconnect.entity;

import jakarta.persistence.Entity;

@Entity
public class TimeSlot { //Table to know time slots that are Available or not for a court.
    //ID, Court, Date, startTime, EndTime, isAvailable
}
