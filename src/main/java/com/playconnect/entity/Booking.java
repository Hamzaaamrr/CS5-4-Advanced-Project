package com.playconnect.entity;

import jakarta.persistence.Entity;

@Entity
public class Booking {
    //ID, ForeignKeys(User, Court, TimeSlot), TotalPrice, BookingStatus(Complete, Pending, Cancelled)
}
